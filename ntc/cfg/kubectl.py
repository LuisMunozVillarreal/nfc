KUBE_SECRETS = {
    "nutrition-api-secret-key": {
        "conf_var": "ApiSecretKey",
        "key_name": "secret-key",
    },

    "nutrition-cloudflare": {
        "conf_var": "CloudflareDnsApiToken",
        "key_name": "dns-token",
    },
}
